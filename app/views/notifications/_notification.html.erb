<%= turbo_frame_tag dom_id(notification) do %>
  <%=link_to  notification.notifiable, id: notification.id, class: "dropdown-item", "data-turbo-frame": "_top" do %>
    <p>
      <%= "#{notification.action} #{notification.id}"%>
    </p>
  <% end %>
<% end %>
